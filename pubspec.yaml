name: lecle_cache
description: Cache with Key-Value
version: 0.0.1
author: kthangtd
publish_to: none

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: '>=2.0.0 <=3.0.0'

  # security
  lecle_crypto: 
    git:
      url: git://github.com/kthangtd/lecle_crypto.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: